pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                echo 'Building the project...'
                // Add build commands like Maven, Gradle, etc.
            }
        }
        stage('Test') {
            steps {
                echo 'Running tests...'
                // Add test commands (e.g., JUnit, Selenium)
            }
        }
        stage('Deploy') {
            steps {
                echo 'Deploying the project...'
                // Add deploy commands (e.g., Docker, AWS)
            }
        }
    }
}
